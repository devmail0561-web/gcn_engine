# Corpus de phrases françaises réelles — GCN-Core
# Format : une phrase par ligne, commentaires avec #
# Sélection : 40 phrases couvrant les 8 patterns causaux du schéma gcn-nl
# Source : presse, Wikipedia, textes académiques
#
# Workflow :
#   1. Ajouter 40 phrases (supprimer les lignes de commentaires)
#   2. Lancer : gcn-bootstrap --input phrases_fr.txt --out-dir gcn-datasets/corpus/ \
#                             --taxonomy-dir gcn-references/taxonomies/ --lang fr
#   3. Réviser les JSON générés (node_type, relation, confidence)
#   4. Valider : gcn-eval --data-dir gcn-datasets/corpus/ --model-path model.npz
#
# Patterns à couvrir (au moins 5 phrases par pattern) :
#   condition     — "Si X, alors Y"
#   cause         — "X provoque Y", "Y parce que X"
#   motivation    — "Pour faire X, il Y"
#   concession    — "Bien que X, Y"
#   sequence      — "Après X, Y", "D'abord X, puis Y"
#   enable        — "X permet à Y de Z"
#   prevent       — "X empêche Y"
#   cause_implicite — juxtaposition sans marqueur
